{
  "name": "wasm-game-of-life-www",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/wasm-game-of-life/pkg && cp index.html bootstrap.js index.js dist/wasm-game-of-life/ && cp -R pkg/. dist/wasm-game-of-life/pkg/"
  }
}
